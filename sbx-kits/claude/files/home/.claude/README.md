# Claude Sandbox Notes

This directory is copied into `/home/agent/.claude/` when the `sbx-kits/claude` kit is used.

The starter kit is intentionally minimal.

Keep durable repo guidance in the repository itself:

- `/workspace/.agents/`
- `/workspace/.claude/`

That way the sandbox setup stays small and the actual repo-specific instructions stay versioned with the project.
