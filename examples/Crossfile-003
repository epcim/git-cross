# upstream
cross use khue	https://github.com/khuedoan/homelab
cross use bill	https://github.com/billimek/k8s-gitops
cross use core https://github.com/habitat-sh/core-plans

git remote -v | grep fetch | column -t

# stash?
# repo_is_clean || say "There are uncommitted changes in the repository. Stash them first." 1

# cross patches
# ex: origin:path/remote/dir [local/path/to/dir] [branch]
cross patch khue:/metal       deploy/metal
cross patch bill:/setup/flux  deploy/flux

# ex: patch same dir on remote to same location in local
cross patch core:asciinema