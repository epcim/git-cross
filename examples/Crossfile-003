# upstream
just cross use khue	https://github.com/khuedoan/homelab
just cross use bill	https://github.com/billimek/k8s-gitops
just cross use core https://github.com/habitat-sh/core-plans

git remote -v | grep fetch | column -t

# stash?
# repo_is_clean || say "There are uncommitted changes in the repository. Stash them first." 1

# cross patches
# ex: origin:path/remote/dir [local/path/to/dir] [branch]
just cross patch khue:/metal       deploy/metal
just cross patch bill:/setup/flux  deploy/flux

# ex: patch same dir on remote to same location in local
just cross patch core:asciinema
